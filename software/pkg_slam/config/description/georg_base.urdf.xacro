<?xml version="1.0"?>
<robot name="bento-box-neo" xmlns:xacro="http://ros.org/wiki/xacro">

<!-- Define robot constants -->
  <xacro:property name="base_width" value="0.4"/>
  <xacro:property name="base_length" value="0.49"/>
  <xacro:property name="base_height" value="0.09"/>

  <xacro:property name="wheel_radius" value="0.055"/>
  <xacro:property name="wheel_width" value="0.04"/>
  <xacro:property name="wheel_zoff" value="0.08"/>

  <xacro:property name="wheel_front_x" value="0.12"/>
  <xacro:property name="wheel_rear_x" value="-0.12"/>

<!-- Robot Base -->
  <link name="base_link">
    <visual>
      <geometry>
        <box size="${base_length} ${base_width} ${base_height}"/>
      </geometry>
      <material name="Red">
        <color rgba="0.7 0.0 0.0 1.0"/>
      </material>
    </visual>
  </link>

<!-- Robot Footprint -->
  <link name="base_footprint"/>

  <joint name="base_joint" type="fixed">
    <parent link="base_footprint"/>
    <child link="base_link"/>
    <origin xyz="0.0 0.0 ${wheel_radius+wheel_zoff}" rpy="0 0 0"/>
  </joint>

<!--  LiDAR  -->
  <link name="laser1_frame">
    <visual>
      <geometry>
        <cylinder radius="0.035" length="0.0575"/>
      </geometry>
      <material name="Gray">
        <color rgba="0.2 0.2 0.2 1.0"/>
      </material>
    </visual>
  </link>

  <joint name="laser1_joint" type="fixed">
    <parent link="base_link"/>
    <child link="laser1_frame"/>
    <origin xyz="-0.2 0.155 0.115" rpy="0 0 ${pi - pi/4}"/>
  </joint>

  <link name="laser2_frame">
    <visual>
      <geometry>
        <cylinder radius="0.035" length="0.0575"/>
      </geometry>
      <material name="Gray">
        <color rgba="0.2 0.2 0.2 1.0"/>
      </material>
    </visual>
  </link>

  <joint name="laser2_joint" type="fixed">
    <parent link="base_link"/>
    <child link="laser2_frame"/>
    <origin xyz="0.2 -0.155 0.115" rpy="0 0 ${-pi/4}"/>
  </joint>

<!-- Wheel macro -->
  <xacro:macro name="wheel" params="prefix x_pos y_reflect">
    <link name="${prefix}_link">
      <visual>
        <origin xyz="0 0 0" rpy="${pi/2} 0 0"/>
        <geometry>
          <cylinder radius="${wheel_radius}" length="${wheel_width}"/>
        </geometry>
        <material name="Gray">
          <color rgba="0.2 0.2 0.2 1.0"/>
        </material>
      </visual>
    </link>

    <joint name="${prefix}_joint" type="fixed">
      <parent link="base_link"/>
      <child link="${prefix}_link"/>
      <origin xyz="${x_pos} ${y_reflect*(base_width/2 + wheel_width/2)} ${-wheel_zoff}" rpy="0 0 0"/>
      <axis xyz="0 1 0"/>
    </joint>
  </xacro:macro>

  <!-- LEFT SIDE -->
  <xacro:wheel prefix="wheel_front_left" x_pos="${wheel_front_x}" y_reflect="1"/>
  <xacro:wheel prefix="wheel_rear_left" x_pos="${wheel_rear_x}" y_reflect="1"/>

  <!-- RIGHT SIDE -->
  <xacro:wheel prefix="wheel_front_right" x_pos="${wheel_front_x}" y_reflect="-1"/>
  <xacro:wheel prefix="wheel_rear_right" x_pos="${wheel_rear_x}" y_reflect="-1"/>

</robot>
